import React from 'react';

const RichMediaHint: React.FC = () => (
  <div className="text-xs text-gray-500 mt-2">
    Supports images, code snippets, and audio in questions.
  </div>
);

export default RichMediaHint;
