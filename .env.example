# ============================================
# Quiz Platform - Environment Configuration
# ============================================

# ==========================================
# DATABASE CONFIGURATION (REQUIRED)
# ==========================================
# MongoDB connection string - Use MongoDB Atlas or local instance
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/quizplatform

# Optional: Local MongoDB credentials (if using docker-compose mongodb service)
# MONGO_USERNAME=admin
# MONGO_PASSWORD=password

# ==========================================
# SECURITY & AUTHENTICATION (REQUIRED)
# ==========================================
# JWT Secret for token signing (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long

# ==========================================
# GOOGLE OAUTH (OPTIONAL)
# ==========================================
# Get credentials from: https://console.cloud.google.com
# OAuth 2.0 Client ID for Google Sign-In
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Note: VITE_GOOGLE_CLIENT_ID is used at BUILD TIME for frontend
# The non-VITE version is used at RUNTIME by the backend

# ==========================================
# AI/ML SERVICES (REQUIRED)
# ==========================================
# Groq API for AI-powered features
# Get your API key from: https://console.groq.com
GROQ_API_KEY=gsk_your_groq_api_key_here

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================
# Application URL (adjust for production deployment)
CLIENT_URL=http://localhost:7860

# Server Port
PORT=7860

# Environment (development | production)
NODE_ENV=production

# ==========================================
# FRONTEND BUILD VARIABLES (Build-time only)
# ==========================================
# API endpoint for frontend (relative path recommended)
VITE_API_URL=/api
